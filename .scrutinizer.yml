build:
  environment:
    php:
      version: 7.1

    node: v8.14.0

  tests:
    override:
      -
        command: 'composer build-code-coverage-clover'
        coverage:
          file: 'output/code-coverage/clover.xml'
          format: 'clover'
